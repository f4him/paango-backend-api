
<!-- include header -->
<%- include('include/_header') %>
<!-- /include header -->

<table class="table">
  <thead>
    <tr>
      <th scope="col">#</th>
      <th scope="col">Name</th>
      <th scope="col">Role</th>
      <th scope="col">Update/Delete</th>
   
    </tr>
  </thead>
  <tbody>
    <% if(data.length){
   
    for(var i = 0; i< data.length; i++) {%>  
    <tr>
      <th scope="row"><%= (i+1) %></th>
      <td><%= data[i].username%></td>
      <td><%= data[i].role%></td>
      <td><%= data[i]._id%></td>
      <td><a href="/user/<%= data[i]._id%>">UPDATE</a><a href="#">D</a></td>
    </tr>
    <% }
             
     }else{ %>
         <tr>
            <td colspan="3">No user</td>
         </tr>
      <% } %>    
    
  </tbody>
  </table>
  <a href="/adduser">Add another user</a>

    <!-- include header -->
    <%- include('include/_footer') %>
    <!-- /include header -->